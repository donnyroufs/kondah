{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "publish": "lerna run tsc && lerna publish"
  },
  "devDependencies": {
    "@types/node": "^15.0.1",
    "lerna": "^4.0.0",
    "typescript": "^4.2.4"
  }
}
