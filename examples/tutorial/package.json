{
  "name": "server",
  "version": "1.0.0",
  "main": "bootstrap.ts",
  "author": "donnyroufs",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only --no-notify -r tsconfig-paths/register src/bootstrap.ts",
    "test": "NODE_ENV=test jest --collect-coverage",
    "test:watch": "NODE_ENV=test jest --watch",
    "lint": "eslint . --ext .ts"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^26.0.23",
    "dotenv": "^9.0.1",
    "jest": "^26.6.3",
    "prettier": "^2.2.1",
    "ts-jest": "^26.5.6",
    "ts-node-dev": "^1.1.6",
    "tsconfig-paths": "^3.9.0",
    "typescript": "^4.2.4"
  },
  "dependencies": {
    "@kondah/core": "^0.0.6",
    "@kondah/http-controller": "^0.0.6"
  }
}
