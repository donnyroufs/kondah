{
  "name": "server",
  "version": "1.0.0",
  "main": "bootstrap.ts",
  "author": "donnyroufs",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only --no-notify -r tsconfig-paths/register src/bootstrap.ts",
    "test": "NODE_ENV=test jest --collect-coverage",
    "test:watch": "NODE_ENV=test jest --watch",
    "lint": "eslint . --ext .ts"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^26.0.20",
    "dotenv": "^8.2.0",
    "jest": "^26.6.3",
    "prettier": "^2.2.1",
    "ts-jest": "^26.5.3",
    "ts-node-dev": "^1.1.6",
    "tsconfig-paths": "^3.9.0",
    "typescript": "^4.2.2"
  },
  "dependencies": {
    "@kondah/core": "^0.0.2-alpha.6",
    "@kondah/express-adapter": "^1.0.0-alpha.5",
    "@kondah/http-controller": "^0.0.2-alpha.6"
  }
}
